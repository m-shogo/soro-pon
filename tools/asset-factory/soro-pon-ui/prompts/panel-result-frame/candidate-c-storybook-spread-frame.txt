Generate an image and save it as a PNG file. Use case: game-ui-asset. Cute Pop mobile-game UI asset in the style of top-grossing cute casual puzzle games: a result/achievement panel frame, a landscape rounded-rectangle card (roughly 4:3 width-to-height), front-facing, isolated object only, designed for 9-slice UI scaling. Design: a storybook double-page-spread style frame in warm cream paper, with a soft hand-drawn colored-pencil border showing gentle layered paper-edge shading, and a small cluster of hand-drawn celebratory doodles (tiny stars, a swirl, a small paper-confetti scatter) confined to the four corners only, mirrored for left-right symmetry. Both the horizontal middle band (top and bottom edges) and the vertical middle band (left and right edges) must stay completely plain and uniform in width with no unique detail, so all four edges can be stretched independently without any seam -- this is a strict 9-slice requirement. The entire center area must remain nearly blank warm cream paper with only faint fiber texture, because a result headline, score breakdown, and buttons will be overlaid there and must stay clearly readable regardless of whether the outcome is a win, a loss, or a draw -- the frame must NOT look victory-specific (no crown, no trophy, no "WIN" or "LOSE" wording, no medal). Color: cream paper body, soft lavender and pastel-yellow pencil accents concentrated at the corners only, harmonizing with an existing white-card-plus-warm-border UI system whose primary CTA is a deep rose-pink jelly button. No text, no letters, no numerals, no icon glyph, no watermark, no logo, no known or existing game characters, no mockup, no screenshot, no surrounding interface, no other objects. The material value must come from tactile hand-drawn paper layering and colored-pencil imperfection that cannot be reproduced as a simple CSS border, gradient, dot pattern, or box shadow. Background: perfectly flat solid high-saturation chroma-key green (#00ff00), no gradient, no shadow, no pattern. Center the panel with generous even green margin on all four sides; it must not touch the image edge. Generate this now and confirm the PNG file was saved with its dimensions.
