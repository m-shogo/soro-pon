Generate an image and save it as a PNG file. Use case: game-ui-asset. This is an original mobile-game UI badge background asset for a fictional skin called "Yorunoshirube" (an entirely original game setting, not based on any real commercial game, anime, or manga): a small, front-facing, isolated, horizontal nine-slice badge background object -- a tiny atlas index tab, the kind of small paper tab pasted onto the edge of a hand-inked night atlas page to mark a section -- viewed flat and centered on a solid chroma-key green background (#00ff00) with a narrow and even transparent extraction margin on all sides, no surrounding scene, no mockup, no screenshot, no interface chrome, no existing game UI copy. Design: thin waxed paper, a slightly uneven hand-inked black border along the top and bottom edges only, one very small amber ink dot near one short end as the only accent, otherwise a quiet, calm, nearly flat aged-ivory center with only faint paper-fiber texture. This is not a button and not a large panel and not a ribbon: it is a small flat rectangular index tab with straight sides, no ribbon tails, no deep side notches, no rounded pill shape. This visual quality must come from real waxed-paper texture, irregular hand-inked line work, and print-registration-like imperfection -- it must not be reproducible as a simple CSS solid color, gradient, border, rounded rectangle, pseudo-element, noise overlay, dot pattern, stripe pattern, or box shadow. Nine-slice constraints: all unique detail (the ink dot, any edge irregularity) must remain inside the corner zones only; the horizontal middle bands (top and bottom edges) must be visually uniform along their full width; the vertical middle bands (left and right edges) must be visually uniform along their full height; the central content area must contain no unique motif, staying quiet and calm because dynamic text and an icon will be overlaid on top of it by the game UI; the silhouette must remain clearly recognizable as a small tab when rendered at a tiny size such as 24x20 pixels; avoid any deep side notches or thin protrusions that would collapse into noise at minimum size. Color palette: deep ink-black for the thin border and edge irregularity, aged ivory as the dominant calm center tone, one small warm amber accent dot only, no other colors. No text, no letters, no numerals, no logo, no watermark, no known characters, no ribbon shape, no ticket shape, no green fringe at the edges. This is not a color-recoloring of any ribbon-tab or jelly-candy design; it is an entirely different flat waxed-paper index-tab silhouette. Generate this now and confirm the PNG file was saved with its dimensions.