Generate an image and save it as a PNG file. Use case: game-ui-asset. Cute Pop mobile-game UI asset in the style of top-grossing cute casual puzzle games: a modal dialog panel background, a landscape rounded-rectangle card (roughly 4:3 width-to-height), front-facing, isolated object only, designed for 9-slice UI scaling. Design: a kraft-paper toy-box label card, with visible paper fiber texture and a faint worn sticker-peel mark near each of the four rounded corners only (mirrored for left-right symmetry), bordered by a hand-drawn wavy craft-tape edge line with natural imperfection. Both the horizontal middle band (top and bottom edges) and the vertical middle band (left and right edges) must stay completely plain and uniform in width with no unique detail, so all four edges can be stretched independently without any seam -- this is a strict 9-slice requirement. The entire center area must remain nearly blank kraft paper with only the faint fiber texture, because dialog title, body text, and buttons will be overlaid there and must stay easy to read even when the panel is stretched very tall for long text. Color: warm kraft-cream paper body, soft muted lavender used sparingly for the tape-edge line and corner accents, harmonizing with an existing white-card-plus-warm-border UI system. No text, no letters, no numerals, no icon glyph, no watermark, no logo, no known or existing game characters, no mockup, no screenshot, no surrounding interface, no other objects. The material value must come from tactile hand-crafted paper texture and imperfect tape-edge linework that cannot be reproduced as a simple CSS border, gradient, dot pattern, or box shadow. Background: perfectly flat solid high-saturation chroma-key green (#00ff00), no gradient, no shadow, no pattern. Center the panel with generous even green margin on all four sides; it must not touch the image edge. Generate this now and confirm the PNG file was saved with its dimensions.
