Generate an image and save it as a PNG file. Use case: game-ui-asset. Cute Pop mobile-game UI asset in the style of top-grossing cute casual puzzle games: a primary call-to-action button background, a wide landscape rounded-rectangle button (roughly 10:3 width-to-height), front-facing, isolated object only. Design: a deep rose-pink button that looks like a frosted sweet -- bordered by a piped cream icing line just inside the edge, softly dimensional like real icing with gentle highlight and shadow (this icing depth must NOT look like a flat CSS outline), over a softly airbrushed rose-pink body with subtle inner shading for depth. The icing line must have PERFECTLY UNIFORM width, and along the top and bottom of the horizontal middle section it must run perfectly straight and level with no waves and no unique detail, so the middle can later be stretched horizontally without any seam -- this is for 9-slice UI scaling; any playful icing character may appear only within the rounded end-zones and must be mirrored left-right. All four rounded corners perfectly uniform and symmetric. The face must stay dark enough that white text overlaid later reaches strong readable contrast. Color: deep rose-pink (#c22f57 direction) body, warm cream icing border. No text, no letters, no numerals, no icon glyph, no watermark, no logo, no existing game characters or logos, no mockup, no screenshot, no surrounding interface, no other objects. Background: perfectly flat solid high-saturation chroma-key green (#00ff00), no gradient, no shadow bleeding into the green. Center the button with generous even green margin on all four sides; it must not touch the image edge. Generate this now and confirm the PNG file was saved with its dimensions.
